workflowSteps:
  - name: export-secret
    phase: succeeded
expectations:
  - apiVersion: v1
    kind: Secret
    name: my-secret
    fields:
      metadata.labels.config.oam.dev/catalog: "velacore-config"
