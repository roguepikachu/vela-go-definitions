kind: Application
apiVersion: core.oam.dev/v1beta1
metadata:
  name: test-config-delete
  namespace: default
spec:
  components: []
  workflow:
    steps:
      - name: delete-config
        type: delete-config
        properties:
          name: test
